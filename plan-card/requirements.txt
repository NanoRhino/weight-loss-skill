# Python dependencies for the plan-card skill (and plan-export's
# WeasyPrint-based scripts).
#
# plan-card/scripts/plan-to-image.py (MMS plan card pipeline):
weasyprint>=60
pymupdf>=1.24
# Optional SMSTO QR claim block (pure python, SVG output only — no PIL or
# binary system deps; imported lazily, only when the input JSON carries `qr`):
qrcode>=7.4
#
# plan-export/scripts/generate-pdf.py / generate-html.py (Markdown → PDF/HTML):
markdown>=3.4
#
# NOTE (deployment): WeasyPrint requires pango/cairo/gdk-pixbuf SYSTEM
# libraries — pip alone is not enough.
#   Amazon Linux / EC2:  sudo dnf install -y pango cairo gdk-pixbuf2
#   Ubuntu/Debian:       sudo apt-get install -y libpango-1.0-0 \
#                          libpangocairo-1.0-0 libgdk-pixbuf-2.0-0
#   macOS:               brew install pango cairo gdk-pixbuf libffi
